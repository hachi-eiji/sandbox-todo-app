<%
require 'rack'
require 'json'

begin
req = Rack::Request.new(env)
d = JSON.parse(req.body.read)
rescue
d = {}
end
if d['loginId'] && d['password']
%>
{
  "status": 200,
  "message": "ok"
}
<% else %>
{
  "status": 404,
  "message": "not_found"
}
<% end %>
