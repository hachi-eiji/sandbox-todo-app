<%
require 'rack'
require 'json'

begin
req = Rack::Request.new(env)
d = JSON.parse(req.body.read)
rescue
d = {}
end
if d['loginId'] == 'test' && d['password'] == 'test'
%>
{
  "status": 200,
  "id": "user_exists",
  "message": "ok"
}
<% else %>
{
  "status": 404,
  "id": "login_failed",
  "message": "ログインID・パスワードが間違っています"
}
<% end %>
